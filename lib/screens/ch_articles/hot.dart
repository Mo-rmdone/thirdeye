import 'package:flutter/material.dart';

class HotArticles extends StatelessWidget {
  const HotArticles({super.key});

  @override
  Widget build(BuildContext context) {
    return  Scaffold(
      appBar: AppBar(),
      body: const Text('HotArticles'),
    );
  }
}
