import 'package:flutter/material.dart';

class TopArticles extends StatelessWidget {
  const TopArticles({super.key});


  @override
  Widget build(BuildContext context) {
    return  Scaffold(
      appBar: AppBar(),
      body: const Text('TopArticles'),
    );
  }
}
