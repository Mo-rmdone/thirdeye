import 'package:flutter/material.dart';

class SuggestionArticles extends StatelessWidget {
  const SuggestionArticles({super.key});




  @override
  Widget build(BuildContext context) {
    return  Scaffold(
      appBar: AppBar(),
      body: const Text('SuggestionArticles'),
    );
  }
}
