import 'package:flutter/material.dart';

class ChildrenTrackingScreen extends StatelessWidget {
  const ChildrenTrackingScreen({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return  Scaffold(
      appBar: AppBar(),
      body: const Text('Tracking'),
    );
  }
}
