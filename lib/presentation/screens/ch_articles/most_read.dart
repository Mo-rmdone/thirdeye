import 'package:flutter/material.dart';

class MostReadArticles extends StatelessWidget {
  const MostReadArticles({super.key});






  @override
  Widget build(BuildContext context) {
    return  Scaffold(
      appBar: AppBar(),
      body: const Text('MostReadArticles'),
    );
  }
}
